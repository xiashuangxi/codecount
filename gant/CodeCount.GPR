project CodeCount is
    for Source_Dirs use ("../source");
    for Object_Dir use "../obj";
    for Main use ("cc");

    --package Builder is
    --    for Switches ("Ada") use ("-gnatW8");
    --end Builder;
end CodeCount;